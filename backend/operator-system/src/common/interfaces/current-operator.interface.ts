// src/common/interfaces/current-operator.interface.ts
export interface CurrentOperatorPayload {
  id: number;
  // Add other fields from your JWT payload, e.g.:
  // email: string;
  // companyName: string;
  // role?: string;
}